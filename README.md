# iStore Shop 116 Static

Static presentation build for iStore Shop 116.

## Local run

```powershell
npm install
npm run dev
```

Open `http://localhost:3000/`.

> Note: `server.py` serves the legacy `www/` static dump. Do not use it for the current app.
